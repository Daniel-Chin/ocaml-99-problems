You are probably only interested in the [`./t`](./t) file.  

It is an OCAML semi-auto-tester. It starts a toplevel `ocaml` and pumps whatever is in `./test.ml` to the toplevel.  

Visual inspection of the output is needed.  
